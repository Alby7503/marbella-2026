name = "marbella-bot"
main = "src/index.js"
compatibility_date = "2026-07-01"

# Senza questo, la scheda Logs/Observability della dashboard resta vuota
# anche se le richieste arrivano regolarmente — un Worker creato da riga di
# comando (come questo) non ce l'ha attivo di default, a differenza di uno
# creato dalla dashboard.
[observability]
enabled = true

# Memoria della conversazione. Il bot funziona anche senza (usa solo il
# messaggio citato), ma per ricordarsi gli scambi precedenti serve una KV:
#
#   npx wrangler kv namespace create CHAT_HISTORY
#
# Il comando stampa un id: incollalo qui sotto e togli i commenti.
#
# [[kv_namespaces]]
# binding = "CHAT_HISTORY"
# id = "incolla-qui-l-id-stampato-dal-comando"

# Variabili non segrete. I segreti (TELEGRAM_BOT_TOKEN, GEMINI_API_KEY,
# WEBHOOK_SECRET) NON vanno qui: si impostano come secret del Worker.
[vars]
BOT_USERNAME = "schiavomarbellabot"
PLAN_URL = "https://raw.githubusercontent.com/alby7503/marbella-2026/main/index.html"
GEMINI_MODEL = "gemini-3.5-flash"

# Ricerca web di Gemini. Metti "false" per spegnerla (il bot torna a
# rispondere solo con il piano e la propria conoscenza).
ENABLE_SEARCH = "true"

# TEMPORANEO, per capire perché il bot risponde sempre con lo stesso errore
# generico: mostra il dettaglio tecnico (status HTTP, messaggio) in coda al
# messaggio di errore. Rimetti "false" (o togli la riga) appena trovata la
# causa: un errore interno non va mostrato nel gruppo a regime.
DEBUG_ERRORS = "true"

# ID del gruppo Telegram autorizzato. Vuoto = chiunque trovi il bot può usarlo.
# Scrivi /chatid nel gruppo per farti dire l'ID, poi incollalo qui.
ALLOWED_CHAT_IDS = "-1003985301142"
